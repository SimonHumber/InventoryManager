@model IEnumerable<Inventory.Shared.Dtos.SupplierDto>
@{
    ViewData["Title"] = "Suppliers";
    var search = ViewBag.Search as string;
}

<div class="page-header">
    <div>
        <h1>Suppliers</h1>
        <p class="page-subtitle">Vendors that provide your products</p>
    </div>
    @if (User.IsInRole(IdentitySeeder.AdminRole))
    {
        <a asp-action="Create" class="btn btn-primary"><i class="bi bi-plus-lg"></i> New supplier</a>
    }
</div>

<form class="filter-toolbar" method="get">
    <div class="row g-2 align-items-end">
        <div class="col-md-6 col-lg-4">
            <label class="form-label small text-muted">Search</label>
            <div class="input-group">
                <span class="input-group-text bg-white"><i class="bi bi-search"></i></span>
                <input type="text" name="search" value="@search" class="form-control" placeholder="Name, contact or email" />
            </div>
        </div>
        <div class="col-auto">
            <button class="btn btn-primary" type="submit"><i class="bi bi-funnel"></i> Filter</button>
            @if (!string.IsNullOrWhiteSpace(search))
            {
                <a asp-action="Index" class="btn btn-outline-secondary ms-1">Reset</a>
            }
        </div>
    </div>
</form>

<div class="card">
    <div class="card-body p-0">
        @if (!Model.Any())
        {
            <div class="empty-state">
                <i class="bi bi-truck"></i>
                <p class="mb-0">No suppliers found.</p>
            </div>
        }
        else
        {
            <div class="table-responsive">
                <table class="table table-hover mb-0 align-middle">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Contact</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th class="text-end">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var s in Model)
                        {
                            <tr>
                                <td class="fw-semibold">@s.Name</td>
                                <td>@s.ContactName</td>
                                <td><a href="mailto:@s.Email" class="text-decoration-none">@s.Email</a></td>
                                <td class="text-muted">@s.Phone</td>
                                <td class="text-end">
                                    <a asp-action="Details" asp-route-id="@s.Id" class="btn btn-sm btn-outline-secondary"><i class="bi bi-eye"></i></a>
                                    @if (User.IsInRole(IdentitySeeder.AdminRole))
                                    {
                                        <a asp-action="Edit" asp-route-id="@s.Id" class="btn btn-sm btn-outline-primary"><i class="bi bi-pencil"></i></a>
                                        <a asp-action="Delete" asp-route-id="@s.Id" class="btn btn-sm btn-outline-danger"><i class="bi bi-trash"></i></a>
                                    }
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
    </div>
</div>
